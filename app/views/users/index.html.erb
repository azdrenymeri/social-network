<% @users.each do |user| %>
  
  <% if current_user == user %>
	<% else %>
       <ul>
        <%= user.email %> <%= link_to "Send friend request", friendships_path(user_id: user.id),method: :post %>
       </ul>
		
	<% end %>
   
<% end %>